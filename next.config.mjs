/** @type {import('next').NextConfig} */
const nextConfig = {
	basePath: '/to-dog-list',
	assetPrefix: '/to-dog-list',
	publicRuntimeConfig: {
		basePath: '/to-dog-list',
	},
};

export default nextConfig;
